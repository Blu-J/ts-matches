{
  "name": "ts-matches",
  "version": "5.1.0-1",
  "description": "We want to bring in some pattern matching into the typescript land. We want to be able to type guard a whole bunch of conditions and get the type back.",
  "types": "lib/mjs/matches.d.ts",
  "main": "lib/cjs/matches.js",
  "module": "lib/mjs/matches.js",
  "exports": {
    ".": {
      "import": "./lib/mjs/matches.js",
      "require": "./lib/cjs/matches.js"
    }
  },
  "files": [
    "lib/"
  ],
  "scripts": {
    "ci": "npm run build && npm run test && npm run pretty-check",
    "build": "rm -rf lib && tsc -p tsconfig.json && tsc -p tsconfig-cjs.json && ./fixup.sh",
    "test": "jest",
    "pretty-check": "prettier --check src",
    "fmt": "prettier --write src"
  },
  "author": "Justin Miller",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "fast-check": "^2.18.0",
    "jest": "27.2.4",
    "prettier": "^2.4.1",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.0"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testPathIgnorePatterns": [
      "/src/.*.gen.(jsx?|tsx?)$"
    ],
    "testRegex": "/src/.*.spec.(jsx?|tsx?)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
